{
  "name": "festival",
  "description": "Festival",
  "scripts": {
    "start": "npm run serve | npm run watch",
    "serve": "polymer serve --port 8080",
    "test": "polymer test",
    "watch": "browser-sync start --proxy localhost:8080 --open --files \"**/*\"",
    "build-test": "polymer build && cp -R public build/es6-bundled/public",
    "build": "polymer build",
    "serve-build": "polymer serve build/es6-bundled"
  },
  "dependencies": {
    "@polymer/iron-image": "^3.0.2",
    "@polymer/paper-button": "^3.0.1",
    "@polymer/paper-spinner": "^3.0.2",
    "@polymer/paper-toast": "^3.0.1",
    "@polymer/polymer": "^3.0.0",
    "moment": "^2.24.0",
    "standardized-audio-context": "^23.1.2"
  },
  "devDependencies": {
    "@polymer/test-fixture": "^4.0.2",
    "@webcomponents/webcomponentsjs": "^2.4.3",
    "browser-sync": "^2.26.7",
    "chai": "^4.2.0",
    "eslint": "^6.8.0",
    "husky": "^4.2.5",
    "lint-staged": "^10.1.7",
    "mocha": "^6.2.3",
    "prettier": "1.19.1",
    "wct-mocha": "^1.0.1"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "*.js": "eslint --cache",
    "src/**/*.js": "polymer lint --input",
    "*.{js,json,html,css,md}": "prettier --write"
  }
}
