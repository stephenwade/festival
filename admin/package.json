{
  "name": "festival-admin",
  "description": "Festival admin",
  "license": "ISC",
  "private": true,
  "type": "module",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/stephenwade/festival.git"
  },
  "scripts": {
    "start": "yarn build && node index.js",
    "start:dev": "nodemon --exec \"yarn start\"",
    "lint": "cd .. && yarn lint",
    "format": "cd .. && yarn format",
    "test": "echo \"No tests yet\" && exit 1",
    "clean": "rm -rf ui/dist",
    "build": "yarn clean && cd ui && rollup -c rollup.config.js",
    "deploy": "dotenv -e ../.env -- ./deploy.sh"
  },
  "dependencies": {
    "@koa/router": "12.0.0",
    "@magic-sdk/admin": "1.8.0",
    "@open-wc/building-rollup": "2.2.1",
    "@rollup/plugin-replace": "5.0.2",
    "deepmerge": "4.3.0",
    "dotenv": "16.0.3",
    "koa": "2.14.1",
    "koa-mount": "4.0.0",
    "koa-send": "5.0.1",
    "koa-static": "5.0.0",
    "lit": "2.6.1",
    "rollup": "3.12.0"
  },
  "devDependencies": {
    "dotenv-cli": "7.0.0",
    "nodemon": "2.0.20"
  }
}
